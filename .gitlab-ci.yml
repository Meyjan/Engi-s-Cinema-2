before_script:
  - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'

stages:
  # - test
  - deploy

# Lint
# lint:
#   stage: test
#   script:
#     - phpcbf app
#     - phpcs -n --standard=PEAR app

# Deploy
deploy:dev:
  stage: deploy
  script:
    - cd /opt/lampp/htdocs
    - sudo rm -rf engima
    - cd ~
    - rm -rf engima
    - git clone git@gitlab.informatika.org:if3110-2019-02-k02-01/engima.git
    - sudo cp -r engima /opt/lampp/htdocs
  only:
    - master
    # - development
    - feature/F03-CI-CD